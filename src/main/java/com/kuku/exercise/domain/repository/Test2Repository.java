package com.kuku.exercise.domain.repository;

import com.kuku.exercise.domain.TestEntity2;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Test2Repository extends JpaRepository<TestEntity2, Long> {
}
